*.sql
*.sqlite3
/.env
/.gocache
/.idea
/backups
/public
/setup.json
/tmp
/uploads

# Go command builds like `go build ./cmd/tool-name` drop binaries in the repo root.
# Prefer `go build ./...` when you just want to verify the tree builds cleanly.
/mysql2sqlite
/mysql2sqlite-data
/mysql2sqlite-verify
/gobbs-static
/gobbs-serve
